# Audit Trail

The plugin records tamper-resistant metadata on each post save.

Meta keys written:

_ws_last_edited_by
Most recent editor.

_ws_edit_history
Append-only log of edits.

Each entry records:

user ID
display name
UTC timestamp
