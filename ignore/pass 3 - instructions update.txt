ARCHITECTURAL RULE — NON-NEGOTIABLE:
Shortcodes are presentation layer only. All data retrieval must go through the query layer in query-jurisdiction.php. If you identify a shortcode that calls get_field(), get_post_meta(), or WP_Query directly, flag it before proceeding — do not silently perpetuate the pattern.

We are implementing the instructions findings from a completed ACF audit. The audit report is pasted below. The structural pass has already been completed — field names, types, attributes, and prefixes are current. This pass covers field instructions only.
Read the audit report carefully before reading any files. The analysis is complete — do not re-audit. Implement only what the report identifies under Category 4 — Instructions.

AUDIT REPORT
(paste the full audit report output from Prompt 1 here)

SCOPE OF THIS PASS
Implement all findings flagged in the audit report under Category 4 — Instructions only:

Add missing instructions to fields where the purpose, expected format, or relationship to other fields is not immediately obvious
Revise unclear instructions to be unambiguous and contributor-friendly
Remove unnecessary instructions from fields so self-explanatory that instructions add noise

Do NOT implement any structural changes in this pass — no attribute changes, no type changes, no prefix changes. Those are complete.

INSTRUCTIONS WRITING PRINCIPLES
Instructions are read by contributors who are not lawyers and may not be familiar with the data model. Write for that audience.

Use plain language — if a legal term must appear, briefly define it
Be specific about expected format — dates, codes, URLs, and numeric values should state exactly what format is expected
Where a field relates to another field, name the relationship explicitly — "This field works together with [field label] to define the filing deadline"
Where a field has a downstream render consequence, describe it — "When enabled, this field renders a Plain Language section visible to site visitors"
Where a field is read-only and auto-stamped, say so clearly — "This field is automatically set when the record is saved. Do not edit manually."
Legal concept fields require the most care — exhaustion_required, burden_of_proof, tolling_notes, statute_trigger and similar should include a brief plain-English definition of the concept and guidance on where to find the relevant information in the source statute

Priority fields for instructions — address these first:

Any field flagged as missing instructions in the audit report
Any field involving legal concepts
Any field whose purpose depends on understanding the broader data model
Any autostamp field that currently has no instruction explaining its read-only nature


IMPLEMENTATION ORDER

Read every ACF file in full — confirm the structural pass is complete and field names are current
Read the audit report Category 4 findings
Draft instructions for all flagged fields
Implement in file order — complete one file fully before moving to the next


FILES TO MODIFY:

All ACF files identified in the audit report as having Category 4 findings

The exact file list will be determined by the audit report. Do not modify files with no Category 4 findings.

Before writing any code: Read every ACF file in full to confirm the current state reflects the completed structural pass. Flag any field whose current state differs from what the audit report describes — this may indicate the structural pass introduced changes that affect the instructions context. Do not implement any instructions until the current file state has been confirmed. Treat all instructions changes as a single coordinated pass — complete one file fully before moving to the next, and do not skip any field flagged in the audit report.