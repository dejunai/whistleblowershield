We are mid-session on the WhistleblowerShield.org JSON prompt system rewrite.
We left off finalizing the completed/integrity block for prompt v1.9.0.

The open decision:

`meta.batch_completed` currently serves as the ingest tool completion sentinel.
The third top-level key — previously named "completed" in v1.7.0 — was proposed
to be renamed "integrity" to better reflect its purpose as an honest self-report
of batch errors, separate from the completion signal.

The "integrity" block as last discussed:

{
  "integrity": {
    "with_errors":   false,
    "error_details": [],
    "error_count":   0
  }
}

Decisions still needed:
  1. Confirm "integrity" as the final block name
  2. Confirm batch_completed stays in meta only
  3. Insert the finalized block into prompt v1.9.0
  4. Update json-schema-reference.md with the integrity block

Current working documents are in this session's context. Please review
and pick up from here.